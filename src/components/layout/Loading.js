import { Box, Center, Text } from "native-base";

const Loading = (props) => {
    return (
        <Box>
            <Center>
                <Text>Loading...</Text>
            </Center>
        </Box>
    );
}

export default Loading;