import { Box, Center, Text } from "native-base";

const Loading = (props) => {
    return (
        <Box width="100%" height="100%" >
            <Center>
                <Text>Loading...</Text>
            </Center>
        </Box>
    );
}

export default Loading;