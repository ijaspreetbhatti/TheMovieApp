import { Box, Center, Text } from "native-base";

export default Loading = (props) => {
    return (
        <Box>
            <Center>
                <Text>Loading...</Text>
            </Center>
        </Box>
    );
}