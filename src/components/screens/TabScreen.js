import TabScreenContainer from '../containers/TabScreenContainer'

const TabScreen = ({ navigation, route }) => {
    return (
        <TabScreenContainer navigation={navigation} route={route} />
    )
}

export default TabScreen